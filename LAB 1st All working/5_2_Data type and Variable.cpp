
#include <iostream>   // Includes the input-output stream library

using namespace std;  // Allows us to use standard names like cout without std::

int main()            // Main function - execution starts here
{
    int a = 10;       // Declares an integer variable 'a' and initializes it with value 10

    cout << "a = " << a;   // Prints the text "a = " followed by the value of 'a' (which is 10)

    return 0;         // Indicates the program ended successfully
}


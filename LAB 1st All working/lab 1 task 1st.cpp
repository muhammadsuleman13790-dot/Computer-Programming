#include <iostream>   // This header file allows input and output operations (like cout and cin)

using namespace std;  // This allows us to use standard library names (cout, cin) without writing std::

int main()            // The main() function is the starting point of every C++ program
{
    
    // This program currently does nothing.
    // It is a basic structure of a C++ program.
    // You can add your code here.

    return 0;         // This statement ends the program and returns 0 to the operating system,
                      // which means the program executed successfully.
}